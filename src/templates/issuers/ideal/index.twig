<div class="ideal-issuers-form">
    <select name="issuer_id">
        {% for issuer in issuers %}
            <option value="{{ issuer.code }}">{{ issuer.description }}</option>
        {% endfor %}
    </select>
</div>
